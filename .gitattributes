# Ensure consistent line endings across platforms.
# Snapshot JSON files must stay LF so go-snaps comparisons
# produce identical results on macOS, Linux, and Windows.
testdata/snapshots/**/*.json text eol=lf
